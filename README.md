
# Trading Bot Pro — V8_AI_Trading (Spot / PAPER & LIVE)

بوت تداول سبوت مبني على بايثون مع واجهة **PyQt5 Premium UI** ومحرك تداول يعتمد على إشارات الاستراتيجية وتحليل متعدد الأطر الزمنية مع طبقة قرار AI.  
يدعم:
- ✅ وضع **PAPER** (تجريبي)
- ✅ وضع **LIVE** (حقيقي)
- ✅ إدارة مراكز البوت + عرض الصفقات المفتوحة/المغلقة
- ✅ قراءة مراكز الحساب الحقيقي وعرضها في نافذة مستقلة (تحكم يدوي)
- ✅ نظام مخاطرة وقيود يومية
- ✅ حفظ الحالة والـ Watchlist
- ✅ تكامل **Telegram** للأوامر والتنبيهات
- ✅ سجل مختصر (الأحداث المهمة فقط)

> **تنبيه:** المشروع لأغراض تعليمية وتجريبية. التداول الحقيقي يحمل مخاطر عالية وقد يؤدي إلى خسارة رأس المال.

---

## ✨ نظرة سريعة على المعمارية

**Core**
- `trading_engine.py`  
  العقل المركزي: يربط السوق، الاستراتيجية، إدارة المراكز، المخاطر، وطبقة AI + يجهز دوال Telegram النصية.
- `market_data_manager.py`  
  تجميع أسعار/شموع وإدارة WS/REST.
- `strategy_engine.py`  
  حساب مؤشرات وإشارات الدخول/الخروج.
- `multi_timeframe_analyzer.py`  
  دمج إشارات عدة أطر زمنية.
- `ai_orchestrator.py`  
  تحويل الإشارات والـ MTF إلى قرار ENTRY/EXIT/HOLD.
- `risk_manager.py` (+ `smart_risk_engine.py` إن وُجد)  
  القيود والحماية اليومية وإدارة الأحجام.
- `position_manager.py`  
  فتح/تحديث/إغلاق الصفقات وتحديث PnL.
- `settings_manager.py` / `state_manager.py`  
  إعدادات + حالة محفوظة (JSON).
- `telegram_bot.py`  
  قناة التحكم والتنبيهات.
- `logger.py`  
  السجل وفلترة المستويات.

**UI**
- `ui/main_window.py` مع صفحات:
  - Home
  - Watchlist
  - Strategies
  - Positions
  - Settings
  - Logs

---

## 📁 هيكلة المشروع (مقترحة)

```text
V8_AI_Trading/
├─ core/
│  ├─ trading_engine.py
│  ├─ strategy_engine.py
│  ├─ market_data_manager.py
│  ├─ position_manager.py
│  ├─ risk_manager.py
│  ├─ smart_risk_engine.py
│  ├─ multi_timeframe_analyzer.py
│  ├─ ai_orchestrator.py
│  ├─ settings_manager.py
│  ├─ state_manager.py
│  ├─ logger.py
│  ├─ api_keys.py
│  └─ telegram_bot.py
│
├─ ui/
│  ├─ main_window.py
│  ├─ home_page.py
│  ├─ strategies_page.py
│  ├─ coin_details_dialog.py
│  ├─ manual_close_dialog.py
│  ├─ sound_alerts.py
│  ├─ popup_notifications.py
│  └─ watchlist_integrations.py
│
├─ data/
│  ├─ sounds/
│  └─ state/
│
├─ fonts/
├─ settings.json
├─ requirements.txt
└─ README.md
```

---

## ⚙️ المتطلبات

- Python 3.10+ (يفضل 3.11)
- Windows / Linux / macOS
- حساب Binance Spot (اختياري لوضع LIVE)

تثبيت الاعتماديات:

```bash
pip install -r requirements.txt
```

---

## 🚀 التشغيل

تشغيل الواجهة:

```bash
python ui/main_window.py
```

أو إن كان لديك ملف تشغيل رئيسي:

```bash
python main.py
```

---

## 🔐 مفاتيح Binance

المشروع يفصل مفاتيح API عن بقية الإعدادات لأمان أفضل.  
أضفها من صفحة Settings أو وفق التنفيذ الموجود في:

- `core/api_keys.py`

> لا تشارك ملفات المفاتيح أو ترفعها إلى GitHub.

---

## 🧪 وضع PAPER

- لا يتم إرسال أوامر حقيقية إلى Binance.
- الرصيد التجريبي يعتمد على:

```json
{
  "paper": {
    "initial_balance": 100.0
  }
}
```

---

## ✅ وضع LIVE

- يحتاج مفاتيح صحيحة.
- يمكنك تفعيل/تعطيل testnet من Settings:

```json
{
  "binance": {
    "use_testnet": false
  }
}
```

---

## 📌 Watchlist

يمكن تعديل قائمة المراقبة من الواجهة أو عبر `state.json`:

```json
{
  "watchlist": ["BTCUSDT", "ETHUSDT", "XRPUSDT"]
}
```

---

## 🤖 أوامر Telegram (مقترحة)

هذه الأوامر تعتمد على نسخة `TelegramBot` المطوّرة لديك التي تقوم بعمل parsing وربطها بدوال `TradingEngine`.

- `/status` حالة البوت
- `/startbot` تشغيل البوت
- `/stopbot` إيقاف البوت
- `/pnl` الربح والخسارة اليوم
- `/mode` تبديل PAPER/LIVE
- `/open` الصفقات المفتوحة حالياً + PnL
- `/capital` الرصيد المستخدم والمتبقي
- `/ledger` Capital + Bot Balance

داخل `TradingEngine` توجد دوال مساعدة مثل:
- `telegram_text_status()`
- `telegram_text_pnl()`
- `telegram_text_open_positions()`
- `telegram_text_capital()`
- `telegram_text_balances_ledger()`
- `telegram_start_bot()`
- `telegram_stop_bot()`
- `telegram_toggle_mode()`

---

## 🧾 سجل مختصر لتخفيف التهنيق

إذا كان السجل كبيراً وسريعاً، اجعل اللوق **يسجل المهم فقط**:
- ✅ فتح صفقة
- ✅ إغلاق صفقة
- ⚠️ رفض فتح صفقة (مع السبب)

اقتراح سريع:
- أضف فلتر في `core/logger.py` أو داخل `TradingEngine`
بحيث يتم تجاهل DEBUG الخاص بالإشارات والشموع.

---

## 🧠 لماذا لا يدخل البوت صفقات؟

أسباب شائعة:
1) نقص بيانات شموع لبعض الأطر (خصوصاً 4h / 1d).  
2) شرط عدد إشارات MTF أعلى من الواقع الحالي للسوق.  
3) قيود `RiskManager` (daily max loss / max open positions / min trade).  
4) حد أدنى لقيمة الصفقة أو MIN_NOTIONAL.  
5) رصيد Paper غير كافٍ.  
6) وجود صفقة Bot مفتوحة لنفس الرمز.

أفضل حل للتشخيص:
✅ **Debug Reasons Log**  
كل مرة يُرفض الدخول، يكتب السبب بوضوح (مثلاً:  
`ENTRY_REJECT: Not enough valid signals (2/3)` أو  
`ENTRY_REJECT: req_usdt too small`).

---

## 🧩 ضبط المرونة

لجعل الدخول أكثر مرونة:
- خفف شرط عدد توافق الإشارات في `multi_timeframe_analyzer.py`
- راجع حدود `risk_manager.py`
- قلل حد الصفقة الدنيا إذا كان مناسباً لطريقة تداولك

---

## 🛡️ أمان

- لا تشارك مفاتيحك.
- استخدم Testnet للتجربة قبل LIVE.
- احتفظ بنسخ احتياطية من `settings.json` و `state.json`.

---

## 🧭 أفكار تطوير

- Backtester كامل
- إدارة OCO للـ LIVE
- Profiles متعددة للإعدادات
- صفحة AI Tuning داخل الواجهة
- تحسين أداء Market WS وتقليل الحمل

---

## 📄 الرخصة

غير محددة حالياً — الاستخدام شخصي/تعليمي ما لم تتم إضافة رخصة لاحقاً.

